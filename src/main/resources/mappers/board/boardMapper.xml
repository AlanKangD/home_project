<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.care.root.mybatis.board.BoardMapper">
	<resultMap type="com.care.root.board.dto.BoardDTO" id="board">
		   <id property="writeNo" column="write_no"/>
		   <result property="title" column="title"/>
		   <result property="content" column="content"/>
		   <result property="saveDate" column="savedate"/>
		   <result property="hit" column="hit"/>
		   <result property="imageFileName" column="image_file_name"/>
		   <result property="id" column="id"/>
    </resultMap>

	<select id="boardAllList" resultMap="board">
		select * from m_board
	</select>
	<insert id="writeSave">
	 	insert into m_board(write_no,id,title,content,image_file_name)
    		values(m_board_seq.nextval,#{id},#{title},#{content},#{imageFileName})
	</insert>
	<select id="contentView" resultMap="board">
		select * from m_board where write_no = #{writeNo}
	</select>
	<update id="upHit">
		update m_board set hit = hit + 1 where write_no = #{writeNo}
	</update> 
	<update id="modify">
		update m_board set title=#{title}, content=#{content}, 
		   	image_file_name=#{imageFileName} where write_no=#{writeNo}
	</update>
	<delete id="delete">
		delete from m_board where write_no = #{writeNo}
	</delete>
	<insert id="addReply">
	   insert into reply01(id,title,content,write_group) 
	      values(#{id},#{title},#{content},#{write_group})
	</insert>
</mapper>